# This file is a template, and might need editing before it works on your project.
# Official framework image. Look for the different tagged releases at:
# https://hub.docker.com/r/library/node/tags/

# deploy_development:
#   type: deploy
#   environment:
#     name: development
#     url: api.quickplay.app
#   script:
#     - ssh $SERVER_USER@$SERVER_HOST "cd /data1/opt/emtiv-backend && sudo git pull origin development-mobile-api && sudo pm2 reload all && exit"
#   only:
#     - development-mobile-api

stages:
  - deploy

deploy:
  stage: deploy
  image: alpine
  before_script:
      - apk add openssh-client
      - eval $(ssh-agent -s)
      - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
      - mkdir -p ~/.ssh
      - chmod 700 ~/.ssh
  script:
    - ssh -p 40000 administrator@103.253.113.215 "cd /data1/opt/emtiv-backend"