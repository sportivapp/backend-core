const Create = require('./Create');
const Update = require('./Update');
const View = require('./View');
const List = require('./List');
const Delete = require('./Delete');

module.exports = { Create, Update, View, List, Delete };